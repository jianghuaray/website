# 自用导航网站 - 需求文档

> 版本：v2.2
> 日期：2026-05-13
> 状态：待开发

---

## 一、项目概述

| 项目     | 说明                               |
| -------- | ---------------------------------- |
| 项目名称 | 自用导航网站                       |
| 使用者   | 个人（仅作者本人使用）             |
| 部署环境 | 服务器（Nginx / Apache）           |
| 技术架构 | 前端 + 轻量后端                    |
| 数据存储 | 服务器端（JSON 文件或轻量数据库）  |
| 访问保护 | 管理操作需密码验证                 |

---

## 二、页面结构

### 2.1 整体布局

所有链接在一个页面上展示，采用**左侧锚点导航 + 右侧分区内容**的布局。

```
┌──────────────────────────────────────────────────────┐
│  网站标题 / Logo                                      │
├──────────┬───────────────────────────────────────────┤
│          │                                           │
│  分类    │  ── 常用 ──────────────────────────        │
│  导航    │                                           │
│          │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  · 常用  │  │ 图标 │ │ 图标 │ │ 图标 │ │ 图标 │    │
│  · 学习  │  │ 名称 │ │ 名称 │ │ 名称 │ │ 名称 │    │
│  · 工作  │  └──────┘ └──────┘ └──────┘ └──────┘    │
│  · 政务  │                                           │
│  · 工具  │  ── 学习 ──────────────────────────        │
│  · 其他  │                                           │
│          │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│          │  │ 图标 │ │ 图标 │ │ 图标 │ │ 图标 │    │
│          │  │ 名称 │ │ 名称 │ │ 名称 │ │ 名称 │    │
│          │  └──────┘ └──────┘ └──────┘ └──────┘    │
│          │                                           │
│          │  ── 工作 ──────────────────────────        │
│          │  ...                                      │
│          │                                           │
├──────────┴───────────────────────────────────────────┤
└──────────────────────────────────────────────────────┘
```

### 2.2 左侧分类导航

- 固定在页面左侧，不随页面滚动
- 列出 6 个分类，点击后页面平滑滚动到对应分区
- 当前所在分类高亮显示
- 手机端收起为顶部横向滚动条或汉堡菜单

### 2.3 右侧内容区

- 按分类分区展示，每个分区有分类标题
- 每个分区下方是该分类的卡片网格
- 所有分区纵向排列，一页看完所有链接

### 2.4 分类列表

| 序号 | 分类名称 | 用途说明             |
| ---- | -------- | -------------------- |
| 1    | 常用     | 每天必用的网站       |
| 2    | 学习     | 教程、文档、课程平台 |
| 3    | 工作     | 办公、协作工具       |
| 4    | 政务     | 政府网站、办事大厅   |
| 5    | 工具     | 效率工具、在线工具   |
| 6    | 其他     | 不属于以上分类的     |

### 2.5 响应式断点

| 屏幕宽度       | 左侧导航 | 右侧卡片列数 | 说明         |
| -------------- | -------- | ------------ | ------------ |
| ≥ 1200px       | 固定侧栏 | 4 列         | 电脑端       |
| 768px ~ 1199px | 固定侧栏 | 3 列         | 平板横屏     |
| 480px ~ 767px  | 顶部横条 | 2 列         | 平板竖屏     |
| < 480px        | 顶部横条 | 2 列         | 手机端       |

---

## 三、卡片设计

### 3.1 卡片字段

| 字段   | 必填 | 说明                                        |
| ------ | ---- | ------------------------------------------- |
| 名称   | 是   | 网站/工具的显示名称                          |
| 链接   | 是   | 完整 URL（如 `https://www.example.com`）     |
| 图标   | 自动 | 根据 URL 自动抓取网站 favicon                |
| 分类   | 是   | 归属哪个分类（常用/学习/工作/政务/工具/其他） |

### 3.2 卡片展示结构（从上到下）

```
┌──────────────────────┐
│                      │
│      ┌──────┐       │
│      │ 图标 │       │  ← 圆角容器，favicon 自动抓取
│      └──────┘       │     加载失败显示默认占位图标
│                      │
│      网站名称        │  ← 加粗，单行显示，超出显示省略号
│                      │
└──────────────────────┘
```

### 3.3 点击行为

- 整张卡片为可点击区域
- 点击后**新窗口**打开目标链接（`target="_blank"`）
- 鼠标悬停时显示手型光标（`cursor: pointer`）

---

## 四、管理模式

### 4.1 入口

- 点击左侧品牌区域可进入管理模式
- 低调不显眼，不影响正常浏览
- 点击后弹出密码输入框

### 4.2 密码验证

- 弹出密码输入框（非页面跳转）
- 输入正确密码后进入管理模式
- 密码错误提示「密码错误」
- 连续错误 5 次后锁定 10 分钟
- 密码在后端配置文件中设置，不硬编码在前端

### 4.3 管理模式下的变化

进入管理模式后，页面出现以下变化：

| 变化 | 说明 |
|------|------|
| 卡片操作 | 每张卡片右上角出现「编辑」和「删除」按钮 |
| 添加按钮 | 每个分类标题旁出现「+ 添加」按钮 |
| 视觉提示 | 页面顶部显示一条提示条：「管理模式 · 点击品牌区域」 |

### 4.4 退出管理模式

- 再次点击品牌区域
- 或 30 分钟无操作自动退出
- 退出后所有管理按钮消失，恢复为干净的展示页面

---

## 五、功能需求

### 5.1 P0 - 核心功能（必须实现）

| 功能       | 描述                                                         |
| ---------- | ------------------------------------------------------------ |
| 全页浏览   | 一个页面展示所有分类和链接，左侧导航点击平滑滚动到对应分区     |
| 链接跳转   | 点击卡片，在新窗口（`target="_blank"`）中打开目标网站         |
| 管理模式   | 品牌区域入口 → 密码验证 → 进入管理模式                         |
| 添加链接   | 管理模式下，在指定分类中添加新链接                            |
| 删除链接   | 管理模式下，删除指定链接                                      |
| 编辑链接   | 管理模式下，修改链接的名称、URL、分类                   |
| 导出配置   | 一键将全部链接数据导出为 JSON 文件下载到本地                  |

### 5.2 P1 - 重要功能（建议实现）

| 功能       | 描述                                                     |
| ---------- | -------------------------------------------------------- |
| 导入配置   | 支持导入之前导出的 JSON 文件，恢复或合并链接数据         |
| 操作确认   | 删除链接时弹出确认提示，防止误删                         |

### 5.3 明确不需要的功能

| 功能             | 说明     |
| ---------------- | -------- |
| 搜索功能         | 不需要   |
| 暗色/亮色主题切换 | 不需要   |
| 分类拖拽排序     | 不需要   |
| 卡片拖拽排序     | 不需要   |
| 用户注册         | 不需要   |
| 多用户系统       | 不需要   |

---

## 六、后端需求

### 6.1 技术选型（供研发参考）

| 项目     | 建议                                         |
| -------- | -------------------------------------------- |
| 语言     | Node.js / Python / Go 均可                   |
| 框架     | 轻量即可（Express / Flask / Gin 等）         |
| 数据存储 | JSON 文件即可，无需数据库                     |
| 部署     | 与前端同服务器，Nginx 反向代理               |

### 6.2 API 接口

| 方法   | 路径             | 说明         | 鉴权   |
| ------ | ---------------- | ------------ | ------ |
| GET    | /api/links       | 获取所有链接 | 无     |
| POST   | /api/links       | 新增链接     | 需密码 |
| PUT    | /api/links/:id   | 编辑链接     | 需密码 |
| DELETE | /api/links/:id   | 删除链接     | 需密码 |
| POST   | /api/auth        | 密码验证     | 无     |
| GET    | /api/export      | 导出 JSON    | 需密码 |
| POST   | /api/import      | 导入 JSON    | 需密码 |

### 6.3 鉴权机制

- 密码通过 POST `/api/auth` 验证
- 验证通过后返回一个 Token（JWT 或简单 token）
- 后续管理请求在 Header 中携带 Token
- Token 有效期 30 分钟，过期需重新输入密码

### 6.4 数据存储格式

服务器端存储一个 `data.json` 文件：

```json
{
  "links": [
    {
      "id": "唯一标识（时间戳+随机数）",
      "name": "网站名称",
      "url": "https://www.example.com",
      "category": "常用",
      "createdAt": "2026-05-13T10:00:00.000Z",
      "updatedAt": "2026-05-13T10:00:00.000Z"
    }
  ]
}
```

### 6.5 导出 JSON 格式

导出文件名：`nav_backup_YYYYMMDD.json`

```json
{
  "version": "1.0",
  "exportTime": "2026-05-13T10:00:00.000Z",
  "links": [
    {
      "id": "xxx",
      "name": "网站名称",
      "url": "https://www.example.com",
      "category": "常用",
      "createdAt": "2026-05-13T10:00:00.000Z",
      "updatedAt": "2026-05-13T10:00:00.000Z"
    }
  ]
}
```

---

## 七、交互细节

### 7.1 添加链接

- 管理模式下，点击分类标题旁的「+ 添加」按钮
- 弹出表单：

| 字段   | 类型   | 必填 | 校验规则                            |
| ------ | ------ | ---- | ----------------------------------- |
| 名称   | 文本框 | 是   | 不能为空                            |
| 链接   | 文本框 | 是   | 必须以 `http://` 或 `https://` 开头 |
| 分类   | 下拉框 | 是   | 从 6 个分类中选择                   |

- 点击「保存」，调用后端 API，成功后卡片立即出现
- 点击「取消」或点击弹窗外部区域关闭

### 7.2 编辑链接

- 管理模式下，点击卡片右上角「编辑」按钮
- 弹出与添加相同的表单，预填当前数据
- 修改后点击「保存」，调用后端 API 更新

### 7.3 删除链接

- 管理模式下，点击卡片右上角「删除」按钮
- 弹出确认提示：「确定删除该链接？」
- 确认后调用后端 API 删除

### 7.4 导出配置

- 管理模式下可用
- 点击「导出」按钮，自动下载 JSON 文件到本地

### 7.5 导入配置

- 管理模式下可用
- 点击「导入」按钮，选择之前导出的 JSON 文件
- 导入后覆盖当前所有数据

---

## 八、非功能性需求

| 项目       | 要求                                           |
| ---------- | ---------------------------------------------- |
| 响应式     | 手机（320px+）、平板、电脑均可正常使用          |
| 图标容错   | favicon 加载失败时显示默认占位图标              |
| 加载速度   | 首屏加载时间 < 2 秒                            |
| 浏览器兼容 | Chrome、Edge、Safari、Firefox 主流版本          |
| 初始状态   | 网站初始为空，由用户自行添加链接                |
| 数据安全   | 管理操作需密码，API 接口需 Token 鉴权           |
| 备份恢复   | 支持导出/导入 JSON，防止数据丢失                |
